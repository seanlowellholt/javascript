// 5 items to build on inside an object
  // store data
  // display data
    // iterate through stored data
  // add data
    // create an object
  // change data
  // delete data
  // toggleCompleted
  // toggleAll

  var todoList = {
    todos: [];
    displayTodos: function () {
      console.log(this.todos);
    },
    addTodo: function (newTodo) {
      this.todos.push({
        newTodo: newTodo;
        completed: false;
        });
    }
    changeTodo: function (newTodo) {
      this.todos[].newTodo = newTodo;
    }
  }
